# Development & CI dependencies. Install everything (incl. runtime deps) with:
#   pip install -r requirements-dev.txt
-r requirements.txt

# AWS SDK — provided by Lambda at runtime, but needed locally for repo code + tests.
boto3>=1.34
boto3-stubs[dynamodb,s3]>=1.34   # type stubs so `mypy --strict` understands boto3 calls

# Testing
pytest>=8
pytest-cov>=5
moto[dynamodb,s3]>=5             # in-memory AWS mocks — no real account for unit tests

# Lint & types
ruff>=0.5
mypy>=1.10
