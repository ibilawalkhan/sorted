"""Placeholder test that proves the toolchain (pytest + import path) is wired up.

Replace this in M1 with real tests for the Pydantic models and the DynamoDB repo.
Run with: `pytest`  (unit tests only — no AWS account needed).
"""


def test_toolchain_is_wired() -> None:
    assert True
